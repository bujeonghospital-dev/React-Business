# Performance Surgery Schedule - Python API Integration Guide

## 📋 สรุป

ระบบ Performance Surgery Schedule ใช้ **Python API** เป็นแหล่งข้อมูลหลัก:

- **Python API** (Google Sheets) - ดึงข้อมูลจาก Google Sheets คอลัมน์ "Film data" โดยตรงแบบ Real-time
- ข้อมูลถูกต้องและแม่นยำที่สุด

## 🎯 คุณสมบัติหลัก

### 1. Python API Endpoint

- **Route**: `GET /api/film-data`
- **Port**: 5000 (default)
- **Response**: ข้อมูลทั้งหมดจาก Google Sheets "Film data"

### 2. Next.js API Route

- **Route**: `GET /api/surgery-schedule-python`
- **Function**: เชื่อมต่อระหว่าง Frontend และ Python API
- **Cache**: 30 วินาที (ปรับได้)

### 3. Auto-refresh

- ดึงข้อมูลใหม่อัตโนมัติทุก 30 วินาที
- สามารถ refresh ด้วยตนเองได้ทันที

## 🚀 วิธีการใช้งาน

### ขั้นตอนที่ 1: เริ่มต้น Python API

```powershell
# ไปที่โฟลเดอร์ python-api
cd python-api

# ติดตั้ง dependencies (ครั้งแรก)
pip install -r requirements.txt

# รัน Python API
python app.py
```

Python API จะรันที่ `http://localhost:5000`

### ขั้นตอนที่ 2: ตั้งค่า Environment Variables

สร้างไฟล์ `python-api/.env` (ถ้ายังไม่มี):

```env
# Google Sheets Configuration
GOOGLE_SPREADSHEET_ID=your_spreadsheet_id_here
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour_Private_Key_Here\n-----END PRIVATE KEY-----\n"
GOOGLE_PROJECT_ID=your-project-id
GOOGLE_PRIVATE_KEY_ID=your-private-key-id
GOOGLE_CLIENT_ID=your-client-id
GOOGLE_CLIENT_CERT_URL=your-client-cert-url
```

### ขั้นตอนที่ 3: เริ่มต้น Next.js (Terminal แยก)

```powershell
# กลับไปที่ root ของโปรเจค
cd ..

# รัน Next.js
npm run dev
```

### ขั้นตอนที่ 4: เปิดหน้า Performance Surgery Schedule

1. เข้าไปที่ `http://localhost:3000/performance-surgery-schedule`
2. ระบบจะดึงข้อมูลจาก Python API (Google Sheets) อัตโนมัติ

### ขั้นตอนที่ 5: ดูข้อมูล

- ตารางจะแสดงข้อมูลจาก Google Sheets แบบ Real-time
- สามารถ **รีเฟรชข้อมูล** ได้ด้วยปุ่ม "🔄 รีเฟรชข้อมูล"
- ระบบจะแสดงจำนวนข้อมูลและเวลาอัพเดทล่าสุด
- Auto-refresh ทุก 30 วินาที

## 📊 โครงสร้างข้อมูล

### API Response Format

```json
{
  "success": true,
  "data": [
    {
      "id": "film-2",
      "หมอ": "ชื่อหมอ",
      "ผู้ติดต่อ": "สา",
      "ชื่อ": "ชื่อคนไข้",
      "เบอร์โทร": "0812345678",
      "วันที่ได้นัดผ่าตัด": "15/11/2025",
      "เวลาที่นัด": "10:00",
      "ยอดนำเสนอ": "50000",
      "วันที่ผ่าตัด": "20/11/2025",
      "date_consult_scheduled": "15/11/2025",
      "contact_person": "สา",
      "date_surgery_scheduled": "15/11/2025",
      "surgery_date": "20/11/2025"
    }
  ],
  "total": 100,
  "timestamp": "2025-11-13T10:30:00.000Z"
}
```

### คอลัมน์ที่ใช้จาก Google Sheets

| คอลัมน์              | ความหมาย          | ใช้สำหรับ                                  |
| -------------------- | ----------------- | ------------------------------------------ |
| หมอ                  | ชื่อแพทย์         | แสดงในรายละเอียด                           |
| ผู้ติดต่อ            | ผู้ประสานงาน      | จัดกลุ่มตามแถว (101-สา, 102-พิชชา, ฯลฯ)    |
| ชื่อ                 | ชื่อคนไข้         | แสดงในรายละเอียด                           |
| เบอร์โทร             | เบอร์ติดต่อ       | แสดงในรายละเอียด                           |
| วันที่ได้นัดผ่าตัด   | วันที่นัดคอนซัลต์ | **ตาราง P** - วันที่ได้นัดผ่าตัด           |
| เวลาที่นัด           | เวลานัด           | แสดงในรายละเอียด                           |
| ยอดนำเสนอ            | ยอดเงิน           | แสดงในรายละเอียด                           |
| วันที่ผ่าตัด         | วันที่ผ่าตัดจริง  | **ตาราง L** - วันที่ผ่าตัด                 |
| วันที่ได้นัด consult | วันที่นัดคอนซัลต์ | ใช้สำรองถ้าไม่มีคอลัมน์ วันที่ได้นัดผ่าตัด |

## 🔧 ไฟล์ที่เกี่ยวข้อง

### Python API

- `python-api/app.py` - Main Flask application
- `python-api/.env` - Environment variables

### Next.js API Route

- `src/app/api/surgery-schedule-python/route.ts` - API proxy route

### Frontend Component

- `src/app/(fullscreen)/performance-surgery-schedule/page.tsx` - Main page
- `src/app/(fullscreen)/performance-surgery-schedule/styles.css` - Styles
- `src/app/(fullscreen)/performance-surgery-schedule/SurgeryDetailsModal.tsx` - Modal

### Utilities

- `src/utils/pythonApiFilmData.ts` - Python API integration functions
- `src/utils/googleSheets.ts` - Google Sheets types and functions

## 🎨 การทำงานของตาราง

### ตาราง P - วันที่ได้นัดผ่าตัด

- แสดงจำนวนคนไข้ที่ได้นัดคอนซัลต์ในแต่ละวัน
- ใช้ข้อมูลจากคอลัมน์ "วันที่ได้นัดผ่าตัด" หรือ "วันที่ได้นัด consult"
- แสดงตามผู้ติดต่อ (101-สา, 102-พิชชา, ฯลฯ)

### ตาราง L - วันที่ผ่าตัด

- แสดงจำนวนคนไข้ที่ผ่าตัดจริงในแต่ละวัน
- ใช้ข้อมูลจากคอลัมน์ "วันที่ผ่าตัด"
- แสดงตามผู้ติดต่อ (101-สา, 102-พิชชา, ฯลฯ)

## ⚙️ Configuration

### Python API URL

แก้ไขใน `.env.local` (Next.js):

```env
PYTHON_API_URL=http://localhost:5000
```

หรือใช้ค่า default: `http://localhost:5000`

### Cache Duration

แก้ไขใน `src/app/api/surgery-schedule-python/route.ts`:

```typescript
const CACHE_DURATION = 30000; // 30 seconds
```

### Auto-refresh Interval

แก้ไขใน `page.tsx`:

```typescript
const interval = setInterval(() => {
  loadData();
}, 30000); // 30 seconds
```

## 🐛 แก้ไขปัญหา

### ปัญหา: Python API ไม่สามารถเชื่อมต่อได้

**วิธีแก้:**

1. ตรวจสอบว่า Python API กำลังรันอยู่:
   ```powershell
   curl http://localhost:5000/health
   ```
2. ตรวจสอบ Console ใน terminal ที่รัน Python API
3. ตรวจสอบ Environment variables ใน `python-api/.env`

### ปัญหา: ไม่มีข้อมูลแสดงในตาราง

**วิธีแก้:**

1. ตรวจสอบ Google Sheets มีชีท "Film data" หรือไม่
2. ตรวจสอบชื่อคอลัมน์ใน Google Sheets ว่าถูกต้อง
3. ตรวจสอบ Service Account มีสิทธิ์ access Sheet หรือไม่
4. ดู Console log ใน Browser Developer Tools

### ปัญหา: Error 403 - Forbidden

**วิธีแก้:**

1. Share Google Sheet ให้กับ Service Account Email
2. ตรวจสอบว่า Service Account Key ถูกต้อง
3. ตรวจสอบว่า Private Key ใน `.env` มี `\n` แปลงเป็น newline

### ปัญหา: วันที่แสดงผิด

**วิธีแก้:**

1. ตรวจสอบรูปแบบวันที่ใน Google Sheets (แนะนำ: DD/MM/YYYY)
2. ตรวจสอบ timezone settings
3. ดู Console log เพื่อดูค่าวันที่ที่ parse ได้

## 📈 Performance Tips

### 1. Cache Strategy

- Python API มี in-memory cache 30 วินาที
- Next.js API route มี cache 30 วินาที
- สามารถปรับเพิ่มได้ถ้าข้อมูลไม่เปลี่ยนบ่อย

### 2. Auto-refresh

- ปิด auto-refresh ถ้าไม่ต้องการ real-time data
- ใช้ manual refresh แทน

### 3. Data Filtering

- Frontend จะ filter ข้อมูลตามเดือน/ปีที่เลือก
- Python API ส่งข้อมูลทั้งหมดมา (ไม่ filter)

## 🔐 Security Notes

1. **Service Account Key**:

   - เก็บไว้ใน `.env` เท่านั้น
   - อย่า commit ขึ้น Git

2. **API Endpoint**:

   - Python API ควรรันบน internal network
   - ถ้ารัน production ควรเพิ่ม authentication

3. **CORS**:
   - Python API เปิด CORS สำหรับทุก origin
   - Production ควรจำกัด origin

## 📝 Next Steps

### เพิ่ม Features

- [ ] Filter ข้อมูลตามช่วงเวลา
- [ ] Export ข้อมูลเป็น Excel
- [ ] เพิ่ม Charts/Graphs
- [ ] เพิ่มการค้นหาข้อมูล

### Optimization

- [ ] เพิ่ม Redis cache สำหรับ Python API
- [ ] Implement WebSocket สำหรับ real-time updates
- [ ] เพิ่ม pagination สำหรับข้อมูลจำนวนมาก

## 🤝 Support

หากพบปัญหาหรือต้องการความช่วยเหลือ:

1. ดู Console logs ใน Browser และ Python API
2. ตรวจสอบ Network tab ใน Browser DevTools
3. ตรวจสอบ error message ในหน้าเว็บ

## 📚 Related Documentation

- `GOOGLE_SHEETS_SURGERY_SCHEDULE_SETUP.md` - Google Sheets setup
- `PYTHON_API_RAILWAY_GUIDE.md` - Deploy Python API to Railway
- `SERVICE_ACCOUNT_SETUP.md` - Google Service Account setup

---

**สร้างเมื่อ**: 13 พฤศจิกายน 2568  
**เวอร์ชัน**: 1.0  
**สถานะ**: ✅ Ready for Production
