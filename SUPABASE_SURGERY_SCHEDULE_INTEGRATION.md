# 🔗 การเชื่อมต่อ Supabase กับ Performance Surgery Schedule

## 📋 สรุปการอัพเดท

ระบบ **Performance Surgery Schedule** ได้ถูกอัพเดทให้ดึงข้อมูลจาก **Supabase** แทน Google Sheets แล้ว

### ✅ สิ่งที่ได้ทำเสร็จแล้ว

1. ✅ สร้าง Type definitions (`src/types/supabase.ts`)
2. ✅ สร้าง Utility functions (`src/utils/supabaseFilmData.ts`)
3. ✅ อัพเดท `performance-surgery-schedule/page.tsx` ให้ใช้ Supabase
4. ✅ เพิ่ม Environment Variables ใน `.env.local.example`

---

## 🚀 ขั้นตอนการตั้งค่า

### 1️⃣ เพิ่ม Environment Variables

เปิดไฟล์ `.env.local` และเพิ่ม:

```bash
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://houhlbfagngkyrbbhmmi.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here
```

**วิธีหา Supabase Anon Key:**

1. ไปที่: https://supabase.com/dashboard/project/houhlbfagngkyrbbhmmi/settings/api
2. คัดลอก **anon public key**
3. แทนที่ `your-supabase-anon-key-here` ด้วย key ที่คัดลอกมา

---

### 2️⃣ ตรวจสอบว่ามีตาราง `film_data` ใน Supabase

1. ไปที่: https://supabase.com/dashboard/project/houhlbfagngkyrbbhmmi/editor
2. คลิก **Table Editor**
3. ตรวจสอบว่ามีตาราง `film_data` พร้อมข้อมูล

**ถ้ายังไม่มีตาราง:**

- ดูคู่มือที่ `c:\Users\Pac-Man45\Videos\Data\README.md`
- หรือรันไฟล์ SQL ที่ได้จากการ export

---

### 3️⃣ ติดตั้ง Dependencies (ถ้ายังไม่ได้ติดตั้ง)

```bash
npm install @supabase/supabase-js
```

---

### 4️⃣ รีสตาร์ท Development Server

```bash
npm run dev
```

---

## 📊 โครงสร้างไฟล์ใหม่

```
src/
├── types/
│   └── supabase.ts           # Type definitions สำหรับ film_data
├── utils/
│   ├── supabase/
│   │   └── client.ts         # Supabase client (มีอยู่แล้ว)
│   └── supabaseFilmData.ts   # Functions สำหรับดึงข้อมูล surgery schedule
└── app/
    └── (fullscreen)/
        └── performance-surgery-schedule/
            └── page.tsx       # อัพเดทแล้วให้ใช้ Supabase
```

---

## 🔍 การทำงานของระบบ

### ฟิลด์ที่ใช้จาก `film_data` table

ระบบจะดึงข้อมูลจากตาราง `film_data` และแมปไปยัง Surgery Schedule ดังนี้:

| ฟิลด์ใน film_data        | แมปไปยัง           | หมายเหตุ                                |
| ------------------------ | ------------------ | --------------------------------------- |
| `doctor`                 | หมอ                |                                         |
| `contact_person`         | ผู้ติดต่อ          | ใช้แสดงในตาราง (101-สา, 102-พิชชา, ฯลฯ) |
| `customer_name`          | ชื่อ               |                                         |
| `phone_number`           | เบอร์โทร           |                                         |
| `date_surgery_scheduled` | วันที่ได้นัดผ่าตัด | สำหรับตาราง P                           |
| `surgery_date`           | วันที่ผ่าตัด       | สำหรับตาราง L                           |
| `appointment_time`       | เวลาที่นัด         |                                         |
| `proposed_amount`        | ยอดนำเสนอ          |                                         |

### Query ที่ใช้

```typescript
// ดึงเฉพาะข้อมูลที่มีนัดผ่าตัดหรือวันที่ผ่าตัด
await supabase
  .from("film_data")
  .select("*")
  .or("date_surgery_scheduled.not.is.null,surgery_date.not.is.null");
```

---

## 🎯 ฟีเจอร์ที่ใช้งานได้

### ✅ Performance Surgery Schedule

1. **ตาราง P (วันที่ได้นัดผ่าตัด)**

   - แสดงจำนวนการนัดผ่าตัดแต่ละวันแยกตามผู้ติดต่อ
   - ใช้ข้อมูลจาก `date_surgery_scheduled`

2. **ตาราง L (วันที่ผ่าตัด)**

   - แสดงจำนวนผ่าตัดจริงแต่ละวันแยกตามผู้ติดต่อ
   - ใช้ข้อมูลจาก `surgery_date`

3. **Modal รายละเอียด**

   - คลิกที่เซลล์ที่มีข้อมูลเพื่อดูรายละเอียด
   - แสดงชื่อ, เบอร์โทร, หมอ, เวลานัด, ยอดนำเสนอ

4. **Auto-refresh**
   - รีเฟรชข้อมูลอัตโนมัติทุก 30 วินาที
   - มีปุ่ม Manual Refresh

---

## 🔒 Security (Row Level Security)

**สำหรับ Production:**

ควรตั้งค่า RLS Policies ใน Supabase:

```sql
-- อนุญาตให้อ่านข้อมูลได้ (สำหรับผู้ที่ authenticated)
CREATE POLICY "Allow read access" ON film_data
  FOR SELECT
  USING (true);
```

---

## 🐛 Troubleshooting

### ❌ Error: "Supabase client ไม่พร้อมใช้งาน"

**วิธีแก้:**

1. ตรวจสอบ `NEXT_PUBLIC_SUPABASE_URL` และ `NEXT_PUBLIC_SUPABASE_ANON_KEY` ใน `.env.local`
2. รีสตาร์ท dev server: `npm run dev`

### ❌ Error: "เกิดข้อผิดพลาดในการดึงข้อมูล"

**วิธีแก้:**

1. ตรวจสอบว่ามีตาราง `film_data` ใน Supabase
2. ตรวจสอบว่า RLS policies อนุญาตให้อ่านข้อมูลได้
3. เปิด Browser DevTools → Console เพื่อดู error message

### ❌ ไม่มีข้อมูลแสดงในตาราง

**วิธีแก้:**

1. ตรวจสอบว่าข้อมูลใน `film_data` มีฟิลด์ `date_surgery_scheduled` หรือ `surgery_date`
2. ตรวจสอบว่าฟอร์แมตวันที่เป็น `YYYY-MM-DD` (ISO format)
3. ตรวจสอบว่าฟิลด์ `contact_person` มีค่าที่ตรงกับ CONTACT_PERSON_MAPPING

---

## 📝 ตัวอย่างข้อมูลใน film_data

```json
{
  "id": 1,
  "doctor": "หมอสมชาย",
  "contact_person": "สา",
  "customer_name": "คุณสมศรี",
  "phone_number": "0812345678",
  "date_surgery_scheduled": "2025-11-15",
  "surgery_date": "2025-11-20",
  "appointment_time": "10:00:00",
  "proposed_amount": "50000"
}
```

---

## 📚 เอกสารเพิ่มเติม

- [Supabase Dashboard](https://supabase.com/dashboard/project/houhlbfagngkyrbbhmmi)
- [Supabase JavaScript Client](https://supabase.com/docs/reference/javascript/introduction)
- [คู่มือนำเข้าข้อมูล film_data](c:\Users\Pac-Man45\Videos\Data\README.md)

---

## 🎉 เสร็จสิ้น!

ระบบ Performance Surgery Schedule ตอนนี้เชื่อมต่อกับ Supabase เรียบร้อยแล้ว! 🚀

**ขั้นตอนถัดไป:**

1. ตั้งค่า Environment Variables
2. นำเข้าข้อมูล film_data (ถ้ายังไม่ได้นำเข้า)
3. ทดสอบการทำงานที่ http://localhost:3000/performance-surgery-schedule

---

**สร้างโดย**: GitHub Copilot  
**วันที่**: 11 พฤศจิกายน 2025  
**เวอร์ชัน**: 1.0
