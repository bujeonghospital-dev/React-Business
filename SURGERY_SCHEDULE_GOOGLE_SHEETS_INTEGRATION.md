# Performance Surgery Schedule - Google Sheets Integration

## สรุปการทำงาน

ระบบตารางนัดผ่าตัด (Performance Surgery Schedule) ได้รับการอัพเดทให้ดึงข้อมูลจาก **Google Sheets** แทนที่จะเป็น Supabase

## ฟีเจอร์หลัก

### 1. ดึงข้อมูลจาก Google Sheets

- ชื่อชีท: `Film data`
- ดึงข้อมูลผ่าน Service Account API
- Auto-refresh ทุก 30 วินาที

### 2. ตารางแสดงผล 2 แบบ

#### ตาราง P - วันที่ได้นัดผ่าตัด

- แสดงจำนวนนัดผ่าตัดรายวันของแต่ละผู้ติดต่อ
- ใช้คอลัมน์ `วันที่ได้นัดผ่าตัด` จาก Google Sheets

#### ตาราง L - วันที่ผ่าตัด

- แสดงจำนวนการผ่าตัดจริงรายวันของแต่ละผู้ติดต่อ
- ใช้คอลัมน์ `วันที่ผ่าตัด` จาก Google Sheets

### 3. Mapping ผู้ติดต่อ

ระบบจะแมพข้อมูลจากคอลัมน์ `ผู้ติดต่อ` ไปยังแถวในตารางดังนี้:

| รหัสในตาราง | ชื่อผู้ติดต่อในชีท |
| ----------- | ------------------ |
| 101-สา      | สา                 |
| 102-พิชชา   | พิชชา              |
| 103-ตั้งโอ๋ | ตั้งโอ๋            |
| 104-Test    | Test               |
| 105-จีน     | จีน                |
| 106-มุก     | มุก                |
| 107-เจ      | เจ                 |
| 108-ว่าน    | ว่าน               |
| 109-ไม่ระบุ | ไม่ระบุ            |

หมายเหตุ: ถ้าข้อมูลไม่มี `ผู้ติดต่อ` หรือเป็นค่าว่าง จะถูกจัดเข้าแถว "109-ไม่ระบุ"

### 4. Popup รายละเอียด

เมื่อคลิกที่ช่องที่มีข้อมูล จะแสดง Modal พร้อมรายละเอียดดังนี้:

- 👨‍⚕️ **หมอ** - ชื่อแพทย์
- 👤 **ผู้ติดต่อ** - ผู้ประสานงาน
- **ชื่อ** - ชื่อคนไข้
- 📞 **เบอร์โทร** - หมายเลขติดต่อ
- 📅 **วันที่ได้นัดผ่าตัด** - วันที่นัด (สำหรับตาราง P)
- 📅 **วันที่ผ่าตัด** - วันที่ผ่าตัดจริง (สำหรับตาราง L)
- 🕐 **เวลาที่นัด** - เวลานัด
- 💰 **ยอดนำเสนอ** - ราคาที่เสนอ (แสดงเป็นรูปแบบเงินบาท)

## โครงสร้างไฟล์

```
src/
├── app/
│   └── (fullscreen)/
│       └── performance-surgery-schedule/
│           ├── page.tsx                    # หน้าหลักแสดงตาราง
│           ├── SurgeryDetailsModal.tsx     # Modal แสดงรายละเอียด
│           ├── SurgeryDetailsModal.css     # Styles สำหรับ Modal
│           └── styles.css                  # Styles สำหรับตาราง
├── utils/
│   └── googleSheets.ts                     # Functions สำหรับดึงข้อมูล
└── app/
    └── api/
        └── surgery-schedule/
            └── route.ts                    # API Route สำหรับ Google Sheets
```

## ข้อมูลที่ต้องมีใน Google Sheets

ตรวจสอบให้แน่ใจว่าชีท `Film data` มีคอลัมน์ดังต่อไปนี้:

1. `หมอ` - ชื่อแพทย์
2. `ผู้ติดต่อ` - ชื่อผู้ประสานงาน (สา, พิชชา, ตั้งโอ๋, etc.)
3. `ชื่อ` - ชื่อคนไข้
4. `เบอร์โทร` - หมายเลขโทรศัพท์
5. `วันที่ได้นัดผ่าตัด` - วันที่นัดผ่าตัด (รูปแบบ: DD/MM/YYYY)
6. `เวลาที่นัด` - เวลาที่นัด
7. `ยอดนำเสนอ` - ราคาที่เสนอ
8. `วันที่ผ่าตัด` - วันที่ผ่าตัดจริง (รูปแบบ: DD/MM/YYYY) [Optional]

## Environment Variables ที่ต้องตั้งค่า

ในไฟล์ `.env.local`:

```bash
# Google Sheets API Configuration
GOOGLE_SPREADSHEET_ID=your_spreadsheet_id_here
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key\n-----END PRIVATE KEY-----\n"
```

### วิธีหา Environment Variables:

1. **GOOGLE_SPREADSHEET_ID**:

   - เปิด Google Sheets
   - ดูที่ URL: `https://docs.google.com/spreadsheets/d/{SPREADSHEET_ID}/edit`

2. **GOOGLE_SERVICE_ACCOUNT_EMAIL & PRIVATE_KEY**:

   - ไปที่ [Google Cloud Console](https://console.cloud.google.com/)
   - เลือกโปรเจค
   - ไปที่ IAM & Admin > Service Accounts
   - สร้าง Service Account ใหม่ (ถ้ายังไม่มี)
   - Download JSON key file
   - คัดลอก `client_email` ไปใส่ใน `GOOGLE_SERVICE_ACCOUNT_EMAIL`
   - คัดลอก `private_key` ไปใส่ใน `GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY`

3. **Share Google Sheet กับ Service Account**:
   - เปิด Google Sheets
   - คลิก "Share"
   - เพิ่ม email ของ Service Account
   - ให้สิทธิ์ "Viewer" หรือ "Editor"

## การทำงานของระบบ

### 1. การดึงข้อมูล

```typescript
// ดึงข้อมูลจาก API Route
const data = await fetchSurgeryScheduleData();
```

### 2. การนับจำนวนรายวัน

```typescript
// สำหรับตาราง P (วันที่ได้นัดผ่าตัด)
const countMap = countSurgeriesByDateAndPerson(data, month, year);

// สำหรับตาราง L (วันที่ผ่าตัด)
const countMapL = countSurgeriesByActualDateAndPerson(data, month, year);
```

### 3. การแสดงผล

- ตารางแสดง 31 คอลัมน์สำหรับวันที่ 1-31
- แต่ละเซลล์แสดงจำนวนนัด (badge)
- คลิกเซลล์เพื่อดูรายละเอียดใน Modal

## ฟีเจอร์เพิ่มเติม

- ✅ เลือกเดือน/ปีได้
- ✅ ปุ่มรีเฟรชข้อมูล
- ✅ Auto-refresh ทุก 30 วินาที
- ✅ แสดงเวลาอัพเดทล่าสุด
- ✅ แสดงจำนวนข้อมูลทั้งหมด
- ✅ Loading state และ Error handling
- ✅ Modal ที่ออกแบบสวยงาม
- ✅ รองรับข้อมูลที่ไม่มีผู้ติดต่อ

## การแก้ปัญหา

### ไม่มีข้อมูลแสดง

1. ตรวจสอบ `.env.local` ว่ามี credentials ครบ
2. ตรวจสอบว่า Service Account มีสิทธิ์เข้าถึง Sheet
3. ตรวจสอบชื่อชีทต้องเป็น `Film data`
4. ตรวจสอบว่ามีคอลัมน์ครบถ้วน
5. ดู Console เพื่อดู error message

### วันที่ไม่ตรงกัน

- ระบบรองรับรูปแบบ DD/MM/YYYY (แบบไทย)
- ตรวจสอบว่าวันที่ในชีทเป็นรูปแบบที่ถูกต้อง
- ระบบจะใช้ UTC เพื่อป้องกันปัญหา timezone

### Quota limit

- Google Sheets API มี quota limit
- ระบบมี auto-refresh ทุก 30 วินาที
- ถ้าเกิน quota ให้เพิ่มช่วงเวลา refresh

## สรุป

ระบบนี้ให้คุณสามารถ:

1. ✅ ดูตารางนัดผ่าตัดรายวันแยกตามผู้ติดต่อ
2. ✅ ดูตารางการผ่าตัดจริงรายวัน
3. ✅ คลิกดูรายละเอียดแต่ละนัด
4. ✅ เปลี่ยนเดือน/ปีได้
5. ✅ รีเฟรชข้อมูลได้ทั้งอัตโนมัติและด้วยตัวเอง

ข้อมูลทั้งหมดมาจาก Google Sheets โดยตรง ทำให้สามารถแก้ไขข้อมูลได้ง่ายใน Google Sheets และระบบจะอัพเดทอัตโนมัติ!
