# 📝 สรุปการแก้ไขปัญหา Error 403 และปรับปรุงระบบ

## 🔴 ปัญหาที่พบ

1. **Error 403**: "Method doesn't allow unregistered callers" จาก Google Sheets API
2. **แสดงตารางมากเกินไป**: มีตาราง L และประมาณการรายรับที่ยังไม่ได้ใช้งาน

---

## ✅ สิ่งที่แก้ไขแล้ว

### 1. ปรับปรุง Error Handling ใน `googleSheets.ts`

#### เพิ่มการตรวจสอบ API Configuration

```typescript
// ตรวจสอบว่ามี API Key และ Spreadsheet ID
if (!GOOGLE_SHEETS_API_KEY) {
  throw new Error("Google Sheets API Key not configured...");
}

if (!SPREADSHEET_ID) {
  throw new Error("Spreadsheet ID not configured...");
}
```

#### ปรับปรุง Error Message สำหรับ 403

```typescript
if (response.status === 403) {
  throw new Error(
    "API Key ไม่ถูกต้องหรือไม่มีสิทธิ์เข้าถึง กรุณาตรวจสอบ:\n" +
      "1. API Key ถูกต้องและเปิดใช้งาน Google Sheets API\n" +
      "2. Google Sheet ถูกตั้งค่าให้ทุกคนที่มี link สามารถ 'ดู' ได้\n" +
      "3. Spreadsheet ID ถูกต้อง"
  );
}
```

#### Re-throw Error แทนการ Return []

```typescript
catch (error) {
  console.error("Error fetching surgery schedule data:", error);
  throw error; // เพื่อให้ UI แสดง error
}
```

---

### 2. เพิ่ม Error Display ใน UI (`page.tsx`)

#### เพิ่ม State สำหรับ Error

```typescript
const [error, setError] = useState<string | null>(null);
```

#### จัดการ Error ใน useEffect

```typescript
try {
  const data = await fetchSurgeryScheduleData();
  setSurgeryData(data);
} catch (error: any) {
  console.error("Failed to load surgery schedule data:", error);
  setError(error.message || "เกิดข้อผิดพลาดในการโหลดข้อมูล");
}
```

#### แสดง Error Message พร้อมคำแนะนำ

```tsx
{
  error && (
    <div className="error-display">
      <div className="error-icon">⚠️</div>
      <div className="error-content">
        <h3>เกิดข้อผิดพลาด</h3>
        <p>{error}</p>
        <div className="error-help">
          <strong>แนะนำการแก้ไข:</strong>
          <ol>
            <li>ตรวจสอบไฟล์ .env.local ว่ามี API Key และ Spreadsheet ID</li>
            <li>
              ตรวจสอบว่า Google Sheet ถูกตั้งค่าให้ทุกคนที่มี link สามารถ "ดู"
              ได้
            </li>
            <li>ตรวจสอบว่า API Key เปิดใช้งาน Google Sheets API แล้ว</li>
            <li>
              ดูคู่มือเพิ่มเติมได้ที่ GOOGLE_SHEETS_SURGERY_SCHEDULE_SETUP.md
            </li>
          </ol>
        </div>
        <button onClick={() => window.location.reload()}>
          ลองใหม่อีกครั้ง
        </button>
      </div>
    </div>
  );
}
```

---

### 3. ลบตารางที่ไม่ใช้งาน

#### ลบตาราง "วันที่ฝาด L"

- ลบ HTML table สำหรับ L schedule
- ลบ state `lScheduleRows`

#### ลบตาราง "ประมาณการรายรับ"

- ลบ HTML table สำหรับ revenue
- ลบ state `revenueRows`

#### เหลือเฉพาะตาราง "วันที่ได้นัดผ่า P"

```tsx
{
  /* Table - วันที่ได้นัดผ่า P */
}
<div className="table-section">{/* ... table content ... */}</div>;
```

---

### 4. เพิ่ม CSS สำหรับ Error Display (`styles.css`)

```css
/* Error Display */
.error-display {
  display: flex;
  gap: 20px;
  padding: 30px;
  background: linear-gradient(135deg, #fff5f5 0%, #ffe6e6 100%);
  border-radius: 12px;
  margin-bottom: 20px;
  box-shadow: 0 4px 12px rgba(220, 38, 38, 0.15);
  border: 2px solid #ef4444;
  animation: slideDown 0.3s ease-out;
}

.error-icon {
  font-size: 48px;
}
.error-content h3 {
  color: #dc2626;
}
.error-help {
  background: white;
  padding: 20px;
}
.retry-button {
  background: linear-gradient(135deg, #dc2626 0%, #991b1b 100%);
  /* ... */
}
```

---

### 5. สร้างคู่มือใหม่

#### `HOW_TO_FIX_API_403_ERROR.md`

คู่มือครบถ้วนสำหรับแก้ปัญหา 403 พร้อม:

- ✅ ขั้นตอนละเอียดทุกขั้น
- ✅ ภาพประกอบคำอธิบาย
- ✅ Troubleshooting guide
- ✅ Checklist ตรวจสอบ
- ✅ ตัวอย่างข้อมูล

---

## 🎯 ผลลัพธ์หลังแก้ไข

### ✅ Error Handling ที่ดีขึ้น

1. แสดง error message ที่เข้าใจง่าย
2. ให้คำแนะนำการแก้ไขแบบทีละขั้นตอน
3. มีปุ่ม "ลองใหม่อีกครั้ง" สำหรับ reload

### ✅ UI ที่สะอาดขึ้น

1. เหลือเฉพาะตาราง P ตามที่ต้องการ
2. ลบตารางที่ไม่ใช้งานออก (L และ รายรับ)
3. Error display สวยงามและมีประโยชน์

### ✅ คู่มือที่ครบถ้วน

1. วิธีแก้ปัญหา 403 แบบละเอียด
2. Checklist ตรวจสอบ
3. Troubleshooting guide

---

## 📂 ไฟล์ที่แก้ไข

### แก้ไข

- `src/utils/googleSheets.ts` - ปรับปรุง error handling
- `src/app/(fullscreen)/performance-surgery-schedule/page.tsx` - เพิ่ม error state และลบตารางที่ไม่ใช้
- `src/app/(fullscreen)/performance-surgery-schedule/styles.css` - เพิ่ม error display styles

### สร้างใหม่

- `HOW_TO_FIX_API_403_ERROR.md` - คู่มือแก้ปัญหา 403 อย่างละเอียด

---

## 🚀 วิธีใช้งาน

### 1. ตั้งค่า API Key และ Spreadsheet ID

```env
# .env.local
NEXT_PUBLIC_GOOGLE_SHEETS_API_KEY=your_api_key_here
NEXT_PUBLIC_SPREADSHEET_ID=your_spreadsheet_id_here
```

### 2. Restart Development Server

```powershell
# Stop server (Ctrl+C)
npm run dev
```

### 3. เปิดหน้าเว็บ

```
http://localhost:3000/performance-surgery-schedule
```

---

## 📊 สิ่งที่จะเห็น

### ✅ เมื่อตั้งค่าถูกต้อง

- โหลดข้อมูลได้สำเร็จ
- เห็นตัวเลขใน badge (ช่องสีเขียว)
- คลิกแล้วเปิด popup ได้
- แสดงข้อมูลครบถ้วน

### ⚠️ เมื่อตั้งค่าไม่ถูกต้อง

- แสดง error message ชัดเจน
- บอกวิธีแก้ไขทีละขั้นตอน
- มีปุ่ม "ลองใหม่อีกครั้ง"
- Link ไปหาคู่มือ

---

## 🔍 การแก้ปัญหา

### ยังเจอ Error 403?

อ่านคู่มือ: `HOW_TO_FIX_API_403_ERROR.md`

### ไม่แสดงข้อมูล?

ตรวจสอบ:

1. ชื่อ Sheet = "Film data"
2. ชื่อคอลัมน์ถูกต้อง
3. รูปแบบวันที่ถูกต้อง (DD/MM/YYYY)
4. ค่า "ผู้ติดต่อ" ตรงกับที่แมพไว้

### API Quota Exceeded?

- Google Sheets API มี quota limit
- Free tier: 100 requests per 100 seconds per user
- พิจารณาเพิ่ม cache ในอนาคต

---

## 📚 เอกสารที่เกี่ยวข้อง

1. **HOW_TO_FIX_API_403_ERROR.md** - คู่มือแก้ปัญหา 403
2. **GOOGLE_SHEETS_SURGERY_SCHEDULE_SETUP.md** - คู่มือการตั้งค่าทั่วไป
3. **IMPLEMENTATION_SUMMARY.md** - สรุปการพัฒนาระบบ

---

## ✨ สรุป

การแก้ไขครั้งนี้:

- ✅ แก้ปัญหา Error 403 ด้วย error handling ที่ดีขึ้น
- ✅ เพิ่ม error display ที่เป็นมิตรกับผู้ใช้
- ✅ ลบตารางที่ไม่ใช้งานออก (เหลือเฉพาะตาราง P)
- ✅ สร้างคู่มือแก้ปัญหาที่ครบถ้วน
- ✅ ให้คำแนะนำการแก้ไขแบบทีละขั้นตอน

**ระบบพร้อมใช้งาน!** 🎉
