# 🚀 วิธีการรันระบบ Contact Dashboard

## ขั้นตอนที่ 1: เตรียม Python API

### 1.1 ติดตั้ง Dependencies

```bash
cd python-api
pip install -r requirements.txt
```

### 1.2 ตั้งค่า Environment Variables

สร้างไฟล์ `.env` ใน folder `python-api/`:

```env
GOOGLE_SPREADSHEET_ID=1OdHZNSlS-SrUpn4wIEn_6tegeVkv3spBfj-FyRRxg3Y
GOOGLE_PROJECT_ID=name-tel-dev
GOOGLE_PRIVATE_KEY_ID=your-private-key-id
GOOGLE_SERVICE_ACCOUNT_EMAIL=web-sheets-reader@name-tel-dev.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
GOOGLE_CLIENT_ID=your-client-id
GOOGLE_CLIENT_CERT_URL=https://www.googleapis.com/robot/v1/metadata/x509/your-service-account-email
```

### 1.3 รัน Python API

```bash
python app.py
```

API จะรันที่: `http://localhost:5000`

---

## ขั้นตอนที่ 2: รัน Next.js Application

### 2.1 กลับไปที่ root folder

```bash
cd ..
```

### 2.2 ตรวจสอบ .env.local

ตรวจสอบว่ามี `PYTHON_API_URL=http://localhost:5000` ในไฟล์ `.env.local`

### 2.3 รัน Next.js Dev Server

```bash
npm run dev
```

Application จะรันที่: `http://localhost:3000`

---

## ขั้นตอนที่ 3: เข้าใช้งาน Dashboard

เปิดเบราว์เซอร์และไปที่:

```
http://localhost:3000/contact-dashboard
```

---

## 📋 สิ่งที่ระบบทำ

### Python API (app.py)

1. ✅ อ่านข้อมูลจาก Google Sheets (Film_dev)
2. ✅ กรองเฉพาะ status = "อยู่ระหว่างโทรออก" (Column AS)
3. ✅ ดึงข้อมูลจากคอลัมน์:
   - **Column C (index 2)**: ผลิตภัณฑ์ที่สนใจ
   - **Column F (index 5)**: ชื่อ
   - **Column G (index 6)**: เบอร์โทร
   - **Column H (index 7)**: หมายเหตุ
4. ✅ ส่งข้อมูลในรูปแบบ JSON

### Next.js API Route (/api/film-contacts)

1. ✅ เรียก Python API
2. ✅ จัดการ error และ timeout
3. ✅ รองรับการค้นหา (search parameter)

### Contact Dashboard (/contact-dashboard)

1. ✅ แสดงข้อมูลในตาราง 6 คอลัมน์:
   - ชื่อลูกค้า
   - เบอร์โทร
   - **ผลิตภัณฑ์ที่สนใจ** (ใหม่!)
   - หมายเหตุ
   - สถานะ
   - วันที่ติดต่อ
2. ✅ ระบบค้นหา 4 ช่อง:
   - ชื่อลูกค้า
   - เบอร์โทร
   - **ผลิตภัณฑ์ที่สนใจ** (ใหม่!)
   - หมายเหตุ
3. ✅ Auto-refresh ทุก 30 วินาที
4. ✅ แสดงเฉพาะรายการที่มีสถานะ "โทรออก"

---

## 🐛 Troubleshooting

### ปัญหา: Python API ไม่รัน

- ตรวจสอบว่าติดตั้ง dependencies ครบแล้ว
- ตรวจสอบ `.env` ใน folder `python-api/`
- ตรวจสอบว่า Google Service Account มีสิทธิ์อ่าน Spreadsheet

### ปัญหา: Next.js ไม่เชื่อมต่อกับ Python API

- ตรวจสอบว่า Python API รันอยู่ที่ port 5000
- ตรวจสอบ `PYTHON_API_URL` ใน `.env.local`
- ลอง fetch manual: `curl http://localhost:5000/api/film-call-status`

### ปัญหา: ไม่มีข้อมูลแสดง

- ตรวจสอบว่ามีแถวที่มี status = "อยู่ระหว่างโทรออก" ใน Column AS
- เปิด Console (F12) ดู error messages
- ตรวจสอบ Python API response: เปิด `http://localhost:5000/api/film-call-status`

---

## 📊 ตัวอย่าง API Response

### Python API Response

```json
{
  "success": true,
  "data": [
    {
      "id": "film-2",
      "customerName": "คุณสมชาย ใจดี",
      "phoneNumber": "089-123-4567",
      "product": "Pakku Packaging",
      "remarks": "สนใจผลิตภัณฑ์",
      "status": "outgoing",
      "contactDate": "2025-11-12T10:30:00"
    }
  ],
  "total": 1,
  "timestamp": "2025-11-12T10:30:00"
}
```

---

## ✅ Checklist การทำงาน

- [ ] Python API รันที่ port 5000
- [ ] Next.js รันที่ port 3000
- [ ] เปิดหน้า `/contact-dashboard` ได้
- [ ] เห็นข้อมูลในตาราง
- [ ] คอลัมน์ "ผลิตภัณฑ์ที่สนใจ" แสดงข้อมูลถูกต้อง
- [ ] ระบบค้นหาทำงานได้
- [ ] Auto-refresh ทำงานทุก 30 วินาที
