# 🗺️ Quick Start: Google Maps บนโดเมน

## ✅ เสร็จแล้ว! แผนที่พร้อมใช้งาน

โปรเจกต์ของคุณได้ถูกอัปเกรดให้ใช้ **Google Maps Web Components** แล้ว ซึ่งสามารถใช้งานบนทุกโดเมนได้

## 🚀 การใช้งานทันที

### 1. ตรวจสอบ API Key

API Key ของคุณอยู่ใน `.env.local`:

```bash
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIzaSyB0WCRvbI_4fgT7mDCyxMFCFov9VDF2LB0
```

### 2. Restart Development Server

```powershell
# หยุด server (Ctrl + C)
# จากนั้นรันใหม่:
npm run dev
```

### 3. ทดสอบ

เปิด browser ไปที่:

```
http://localhost:3000/contact-inquiry
```

คุณจะเห็นแผนที่แบบใหม่พร้อม marker สวยๆ! 🎉

## 📍 การเปลี่ยนตำแหน่งแผนที่

แก้ไขใน `src/app/contact-inquiry/page.tsx`:

```typescript
const COMPANY_INFO = {
  headquarters: {
    // ... other fields
    coordinates: { lat: 13.685984091307898, lng: 100.72794861574249 }, // เปลี่ยนที่นี่
  },
  factory: {
    // ... other fields
    coordinates: { lat: 13.685984091307898, lng: 100.72794861574249 }, // เปลี่ยนที่นี่
  },
};
```

### วิธีหาพิกัด:

1. เปิด [Google Maps](https://www.google.com/maps)
2. คลิกขวาที่ตำแหน่งที่ต้องการ
3. คลิกที่ตัวเลขพิกัด (จะคัดลอกอัตโนมัติ)
4. นำมาใส่ใน `{ lat: ..., lng: ... }`

## 🌐 การใช้งานบน Production Domain

### ขั้นตอนที่ 1: เพิ่ม Domain ใน Google Cloud Console

1. ไปที่ [Google Cloud Console](https://console.cloud.google.com/apis/credentials)
2. คลิกที่ API Key ของคุณ
3. ในส่วน **Application restrictions**:
   - เลือก **HTTP referrers (web sites)**
4. เพิ่ม domains:

```
http://localhost:3000/*
https://yourdomain.com/*
https://*.yourdomain.com/*
https://www.yourdomain.com/*
```

5. คลิก **Save**

### ขั้นตอนที่ 2: Deploy

```powershell
# Build
npm run build

# Deploy ตามปกติ
# (Vercel, Netlify, หรือ hosting อื่นๆ)
```

### ขั้นตอนที่ 3: ตั้งค่า Environment Variable

บน hosting platform (เช่น Vercel):

```
Key: NEXT_PUBLIC_GOOGLE_MAPS_API_KEY
Value: AIzaSyB0WCRvbI_4fgT7mDCyxMFCFov9VDF2LB0
```

## 🎨 การปรับแต่ง

### เปลี่ยนระดับซูม

```tsx
<GoogleMapComponent
  zoom={17} // ซูมใกล้ (10-20)
  // หรือ
  zoom={12} // ซูมไกล (1-10)
/>
```

### เปลี่ยนชื่อ Marker

แก้ใน `COMPANY_INFO`:

```typescript
headquarters: {
  title: "ชื่อใหม่ที่นี่",
  // ...
}
```

## 📦 ไฟล์ที่สร้างใหม่

1. ✅ `src/components/GoogleMap.tsx` - Component หลัก
2. ✅ `src/types/google-maps.d.ts` - TypeScript types
3. ✅ `docs/GOOGLE_MAPS_WEB_COMPONENTS.md` - เอกสารเต็ม

## 🔍 Troubleshooting

### แผนที่ไม่แสดง?

1. **ตรวจสอบ Console (F12)**:
   - มี error อะไรไหม?
2. **Restart server**:
   ```powershell
   # Ctrl + C แล้ว
   npm run dev
   ```
3. **เช็ค API Key**:
   - มีใน `.env.local` หรือไม่?
   - ถูกต้องหรือไม่?

### "RefererNotAllowedMapError"?

- เพิ่ม domain ใน Google Cloud Console (ดูขั้นตอนข้างบน)

### Marker ไม่แสดง?

- ตรวจสอบพิกัด:
  - `lat` ต้องอยู่ระหว่าง -90 ถึง 90
  - `lng` ต้องอยู่ระหว่าง -180 ถึง 180

## 💡 ข้อดีของ Web Components

เทียบกับ iframe เดิม:

| คุณสมบัติ      | iframe (เดิม) | Web Components (ใหม่) |
| -------------- | ------------- | --------------------- |
| ความเร็ว       | 🐢 ช้า        | 🚀 เร็ว               |
| ปรับแต่ง       | ❌ จำกัด      | ✅ ได้เต็มที่         |
| Responsive     | ⚠️ พอใช้      | ✅ ดีเยี่ยม           |
| Domain Support | ⚠️ จำกัด      | ✅ ทุก domain         |
| SEO            | ⚠️ พอใช้      | ✅ ดีกว่า             |

## 📚 เอกสารเพิ่มเติม

- [เอกสารเต็ม](./GOOGLE_MAPS_WEB_COMPONENTS.md)
- [Google Maps API Setup](./GOOGLE_MAPS_SETUP.md)

## ✨ เสร็จสิ้น!

แผนที่ของคุณพร้อมใช้งานบนทุกโดเมนแล้ว 🎉

หากมีปัญหา สามารถเปิด issue หรือติดต่อทีมพัฒนาได้เลย!
